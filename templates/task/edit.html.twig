{% extends 'base.html.twig' %}

{% block body %}
	<div class="row w-50 text-center m-auto">
		{{ form_start(form, {'action' : path('task_edit', { 'id' : task.id })}) }}
		{{ form_widget(form) }}
		<div class="mt-3">
			<button type="submit" class="btn btn-success mt-3">Modifier</button>
		</div>
		{{ form_end(form) }}
	</div>
{% endblock %}
