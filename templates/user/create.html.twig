{% extends 'base.html.twig' %}

{% block header_title %}<div class="text-center"><h1>Créer un utilisateur</h1></div>{% endblock %}
{% block header_img %}{% endblock %}

{% block body %}
    <div class="row w-50 text-center m-auto">
        {{ form_start(form, {'action' : path('user_create')}) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success mt-3">Ajouter</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
